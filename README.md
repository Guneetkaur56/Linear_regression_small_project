# Linear_regression_small_project


This project implements Simple Linear Regression to predict a continuous target variable from a single input feature. It includes data preprocessing, exploratory data analysis (EDA), model training, evaluation, and visualization of regression results. The model is built using Python and Scikit-learn, with performance evaluated using standard regression metrics. This project demonstrates the complete workflow of a supervised machine learning regression task.
